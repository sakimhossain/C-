#include<stdio.h>
#include<conio.h>
int main()
{
    int a,b,result;
    printf("enter the first value:");
    scanf("%d",&a);
    printf("enter the second value:");
    scanf("%d",&b);
    result = a*b;
    printf("%d * %d = %d",a*b,result);
    return 0;
}
